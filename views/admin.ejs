<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Admin Panel – Upload Excel</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
* {
  box-sizing: border-box;
  font-family: "Segoe UI", sans-serif;
}

body {
  margin: 0;
  min-height: 100vh;
  background: linear-gradient(135deg, #141e30, #243b55);
  display: flex;
  align-items: center;
  justify-content: center;
}

.admin-card {
  width: 100%;
  max-width: 480px;
  background: #fff;
  padding: 36px 32px;
  border-radius: 16px;
  box-shadow: 0 30px 50px rgba(0,0,0,0.35);
}

.admin-header {
  text-align: center;
  margin-bottom: 26px;
}

.admin-header h2 {
  margin: 0;
  font-size: 28px;
}

.admin-header p {
  margin-top: 6px;
  color: #666;
  font-size: 14px;
}

/* Inputs */
.form-group {
  margin-bottom: 18px;
}

.form-group label {
  display: block;
  font-size: 13px;
  margin-bottom: 6px;
  color: #333;
}

.form-group input[type="text"] {
  width: 100%;
  padding: 14px;
  border-radius: 8px;
  border: 1px solid #ccc;
  font-size: 15px;
}

/* FILE UPLOAD FIX */
.upload-box {
  width: 100%;
  border: 2px dashed #cbd5e1;
  border-radius: 12px;
  padding: 26px;
  text-align: center;
  background: #f8fafc;
  cursor: pointer;
  margin-bottom: 22px;
}

.upload-box:hover {
  background: #eef2f7;
  border-color: #243b55;
}

.upload-box strong {
  display: block;
  font-size: 15px;
  color: #111;
}

.upload-box span {
  display: block;
  margin-top: 6px;
  font-size: 13px;
  color: #666;
}

/* Hide real input COMPLETELY */
.upload-box input {
  display: none;
}

/* Button */
.upload-btn {
  width: 100%;
  padding: 15px;
  border-radius: 10px;
  border: none;
  background: linear-gradient(135deg, #243b55, #141e30);
  color: #fff;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
}

.upload-btn:hover {
  opacity: 0.95;
}

.message {
  margin-top: 20px;
  padding: 14px;
  background: #e8f8f1;
  border: 1px solid #bfe9d7;
  border-radius: 8px;
  color: #1b7f5a;
  font-weight: 600;
  text-align: center;
}

.note {
  margin-top: 18px;
  font-size: 12px;
  color: #888;
  text-align: center;
}
</style>
</head>

<body>

<div class="admin-card">
  <div class="admin-header">
    <h2>Admin Panel</h2>
    <p>Upload Excel file to create a new batch</p>
  </div>

  <form method="POST" action="/admin/upload" enctype="multipart/form-data">

    <div class="form-group">
      <label>Batch Name</label>
      <input
        type="text"
        name="batch_name"
        placeholder="e.g. GrowLabz Hologram – April 2025"
        required
      >
    </div>

    <!-- FIXED FILE UPLOAD -->
    <div class="upload-box" onclick="document.getElementById('excelFile').click()">
  <strong id="uploadText">Click to upload Excel file</strong>
  <span id="uploadSubText">Only .xlsx files supported</span>

  <input
    id="excelFile"
    type="file"
    name="file"
    accept=".xlsx"
    required
    onchange="updateFileName(this)"
  >
</div>


    <button type="submit" class="upload-btn">
      Upload & Import Codes
    </button>

  </form>

  <% if (message) { %>
    <div class="message"><%= message %></div>
  <% } %>

  <div class="note">
    Ensure Excel has a column named <strong>Code</strong>
  </div>
</div>

<script>
  function updateFileName(input) {
    if (input.files && input.files.length > 0) {
      document.getElementById("uploadText").innerText =
        input.files[0].name;

      document.getElementById("uploadSubText").innerText =
        "File selected successfully";
    }
  }
</script>


</body>
</html>
